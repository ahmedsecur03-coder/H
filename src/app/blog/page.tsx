
'use client';

import BlogPageClient from "@/app/(public)/_components/blog-page";

// This page now acts as a simple wrapper for the client component.
export default function BlogPage() {
    return <BlogPageClient />;
}
