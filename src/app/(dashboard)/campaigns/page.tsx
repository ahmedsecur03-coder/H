import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function CampaignsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>مركز الحملات الإعلانية</CardTitle>
      </CardHeader>
      <CardContent>
        <p>سيتم عرض واجهة إدارة الحملات الإعلانية على Google, Facebook, TikTok, و Snapchat هنا.</p>
      </CardContent>
    </Card>
  );
}
