import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function MassOrderPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>طلب جماعي</CardTitle>
      </CardHeader>
      <CardContent>
        <p>سيتم توفير واجهة لإدخال طلبات متعددة دفعة واحدة هنا.</p>
      </CardContent>
    </Card>
  );
}
