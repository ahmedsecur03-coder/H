
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // WARNING: Rules are wide open for development.
    // Anyone can read, write, or delete any data.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
