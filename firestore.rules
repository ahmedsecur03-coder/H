
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // WARNING: Rules are temporarily open for development.
    // Any authenticated user can read or write to any document.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
